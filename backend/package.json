{
  "name": "kaffka-server",
  "description": "A Node.js server application for Kaffka - coffee webstore.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "nodemon -r dotenv/config server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
